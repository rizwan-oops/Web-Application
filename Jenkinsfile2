#!/usr/bin/env groovy

@Library('shared-libraryL@main') _ //master or whatever branch

pipeline{

      agent any
        
        stages{   
		 stage('maven build'){
                  steps{
                     echo 'Success from file '
		       }  
                 }
		
               stage ('Check logs') {
                    steps {
                        filterLogs ('Warning', 50)
                    }
                }
		
           }	       	     	         
}
