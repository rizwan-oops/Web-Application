#!/usr/bin/env groovy

@Library('shared-libraryL@main') _ //master or whatever branch

pipeline{

      agent any
        
        stages{
              stage('maven build'){
                    
                 }	
                 
                 stage ('Check logs') {
                    steps {
                        filterLogs ('WARNING', 50)
                    }
                }
		
           }	       	     	         
}
